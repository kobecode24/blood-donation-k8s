apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: blood-donation-data
  labels:
    app: mysql
    component: database
    tier: data
spec:
  selector:
    app: mysql
  ports:
  - port: 3306
    targetPort: 3306
    protocol: TCP
    name: mysql
  type: ClusterIP
