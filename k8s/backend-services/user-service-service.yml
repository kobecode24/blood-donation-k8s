apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: blood-donation
  labels:
    app: user-service
    component: api
    tier: backend
spec:
  selector:
    app: user-service
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  type: ClusterIP
