apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: blood-donation
  labels:
    app: analytics-service
    component: api
    tier: backend
spec:
  selector:
    app: analytics-service
  ports:
  - port: 3004
    targetPort: 3004
    protocol: TCP
    name: http
  type: ClusterIP
