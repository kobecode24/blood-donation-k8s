apiVersion: v1
kind: Service
metadata:
  name: donor-frontend-service
  namespace: blood-donation
  labels:
    app: donor-frontend
    component: ui
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: donor-frontend
